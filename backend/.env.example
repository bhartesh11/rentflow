# MongoDB connection string
MONGO_URI=mongodb://mongo:27017
MONGO_DB_NAME=rentflow

# JWT
JWT_SECRET=change-this-to-a-long-random-secret-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS - comma separated list of allowed origins
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Initial owner account (auto-seeded on first startup if no owner exists)
OWNER_EMAIL=owner@rentflow.local
OWNER_PASSWORD=changeme123
OWNER_NAME=Property Owner

# Optional: email notifications (SMTP). Leave blank to disable.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@rentflow.local

# Server port
PORT=8000
